<?xml version="1.0" encoding="UTF-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.saxion.ein1b2"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="15" />

    <instrumentation
        android:label="JUnit"
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="nl.saxion.ein1b2" />
 
    <application
        android:icon="@drawable/logo_3"
        android:label="@string/app_name"
        android:theme="@style/tmpTheme" >
        <uses-library android:name="android.test.runner" />
 
        <activity android:name="MenuActivity" >
 
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ActivityVakkenpakketToevoegen" />
        <activity android:name="WizardActivity" />
        <activity android:name="ToetsToevoegenActivity" />
        <activity android:name="PeriodeActivity" />
        <activity android:name="ToetsenOverzichtActivity" />
        <activity android:name="VakOverzichtActivity" />
    </application>

</manifest>